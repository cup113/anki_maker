FROM python:3.11-slim AS backend
WORKDIR /app

COPY server/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY server ./server

RUN mkdir -p /app/output

EXPOSE 4134
CMD ["uvicorn", "server.main:app", "--host", "0.0.0.0", "--port", "4134"]
