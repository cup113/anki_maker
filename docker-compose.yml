# docker-compose.yml
version: '3.8'

services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile.fastapi
    ports:
      - "4134:4134"
    restart: always
    environment:
      - APP_ENV=production
    volumes:
      - generated_files:/app/output
